<p id="notice"><%= notice %></p>

<p>
  <strong>Nombre:</strong>
  <%= @crew.name %>
</p>

<p>
  <strong>Group:</strong>
  <%= @crew.group.name %>
</p>

<%= link_to 'Edit', edit_group_crew_path(@group, @crew) %> |
<%= link_to 'Back', group_crews_path(@group) %>
